apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lldap.fullname" . }}-bootstrap-user
  labels:
    {{- include "lldap.labels" . | nindent 4 }}
data:
{{/* One json file per user */}}
{{- range .Values.users }}
  {{ .id }}.json: | {{ toJson . | nindent 4 }}
{{- end }}
