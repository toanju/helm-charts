apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "lldap.fullname" . }}
  labels:
    {{- include "lldap.labels" . | nindent 4 }}
  {{- with .Values.bootstrapJob.annotations }}
  annotations: 
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  template:
    spec:
      restartPolicy: {{ .Values.bootstrapJob.restartPolicy }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "lldap.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      containers:
        - name: {{ .Chart.Name }}-bootstrap
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - bash
          args:
            - -c
            - |
                /app/bootstrap.sh
          env:
            - name: LLDAP_URL
              value: "http://{{ include "lldap.fullname" . }}:{{ .Values.service.http.port }}"
            - name: LLDAP_ADMIN_USERNAME
              {{- if .Values.externalSecrets.enabled }}
              valueFrom: 
                secretKeyRef: 
                  name: {{ include "lldap.fullname" . }}
                  key: ldap_user_dn
              {{- else }}
              value: "{{ .Values.config.ldap_user_dn }}"
              {{- end }}
            - name: LLDAP_ADMIN_PASSWORD
            {{- if .Values.externalSecrets.enabled }}
              valueFrom: 
                secretKeyRef: 
                  name: {{ include "lldap.fullname" . }}
                  key: ldap_user_pass
            {{- else }}
              value: {{ .Values.config.ldap_user_pass | quote }}
            {{- end }}
            - name: DO_CLEANUP
              value: "true"
          volumeMounts:
            - name: user-schemas
              mountPath: /bootstrap/user-schemas
              readOnly: true
          {{- if .Values.users }}
            - name: user-configs
              mountPath: /bootstrap/user-configs
              readOnly: true
          {{- end }}
            - name: group-schemas
              mountPath: /bootstrap/group-schemas
              readOnly: true
          {{- if .Values.groups }}
            - name: group-configs
              mountPath: /bootstrap/group-configs
              readOnly: true
          {{- end }}
      volumes:
        - name: user-schemas
          {{- if and .Values.externalSecrets.enabled (or .Values.externalSecrets.bootstrapUserSchemas.data .Values.externalSecrets.bootstrapUserSchemas.dataFrom) }}
          secret:
            secretName: {{ include "lldap.fullname" . }}-bootstrap-user-schemas
          {{- else }}
          configMap:
            name: {{ include "lldap.fullname" . }}-bootstrap-user-schemas
          {{- end }}
        {{- if .Values.users }}
        - name: user-configs
          {{- if and .Values.externalSecrets.enabled (or .Values.externalSecrets.bootstrapUser.data .Values.externalSecrets.bootstrapUser.dataFrom ) }}
          secret:
            secretName: {{ include "lldap.fullname" . }}-bootstrap-user
          {{- else }}
          configMap:
            name: {{ include "lldap.fullname" . }}-bootstrap-user
          {{- end }}
        {{- end }}
        - name: group-schemas
          {{- if and .Values.externalSecrets.enabled (or .Values.externalSecrets.bootstrapGroupSchemas.data .Values.externalSecrets.bootstrapGroupSchemas.dataFrom) }}
          secret:
            secretName: {{ include "lldap.fullname" . }}-bootstrap-group-schemas
          {{- else }}
          configMap:
            name: {{ include "lldap.fullname" . }}-bootstrap-group-schemas
          {{- end }}
        {{- if .Values.groups }}
        - name: group-configs
          {{- if and .Values.externalSecrets.enabled (or .Values.externalSecrets.bootstrapGroup.data .Values.externalSecrets.bootstrapGroup.dataFrom) }}
          secret:
            secretName: {{ include "lldap.fullname" . }}-bootstrap-group
          {{- else }}
          configMap:
            name: {{ include "lldap.fullname" . }}-bootstrap-group
          {{- end }}
        {{- end }}
