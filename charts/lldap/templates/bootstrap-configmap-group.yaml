apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lldap.fullname" . }}-bootstrap-group
  labels:
    {{- include "lldap.labels" . | nindent 4 }}
data:
{{/* One json file per group */}}
{{- range .Values.groups }}
  {{ .name }}.json: | {{ toJson . | nindent 4 }}
{{- end }}
