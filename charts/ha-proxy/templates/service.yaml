apiVersion: v1
kind: Service
metadata:
  name: {{ include "ha-proxy.fullname" . }}
  labels:
    {{- include "ha-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "ha-proxy.selectorLabels" . | nindent 4 }}
