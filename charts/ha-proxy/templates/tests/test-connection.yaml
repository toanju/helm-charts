apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ha-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "ha-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ha-proxy.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
